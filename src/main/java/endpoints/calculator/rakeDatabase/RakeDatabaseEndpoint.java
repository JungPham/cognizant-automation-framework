package endpoints.calculator.rakeDatabase;

import shared.*;

public class RakeDatabaseEndpoint extends BaseEndpoint implements IEndpoint{

    public final String path = "/calculator/rakeDatabase";

    public void convertResponseToDataModel(String response) {
    }
}
